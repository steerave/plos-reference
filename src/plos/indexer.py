"""
Vault indexer — runs every 10 minutes; self-cleans the review queue when
unmatched-entity jobs are resolved by manual entity creation.

Phase 5 target.

See ARCHITECTURE.md → "The review queue and the notification layer".
"""
